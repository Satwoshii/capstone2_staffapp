name: syswatch_admin
description: SysWatch Admin intranet management application.
publish_to: 'none'
version: 2.8.0+9

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.1.5
  desktop_drop: ^0.8.4
  file_selector: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo/syswatch_logo.ico
    - assets/logo/syswatch_logo.png